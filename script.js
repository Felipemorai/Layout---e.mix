const input = document.querySelector('.input');
const btn = document.querySelector(#add);

function addList(e) {
    const afazer = document.querySelector('.afazer');
    const feito = document.querySelector('.feito');

    const newLi = document.createElement('li');
    const checkBtn = document.createElement('#checkbox');
    const delBtn = document.createElement('#delete');
    const editBtn = document.createElement('#edit');
}